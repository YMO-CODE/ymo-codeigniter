{
  "name": "ymo-marketing-assets",
  "private": true,
  "description": "Build trimmed Bootstrap CSS for the marketing site",
  "scripts": {
    "build:bootstrap": "node scripts/build-bootstrap-marketing.js",
    "build:bootstrap-js": "node scripts/build-bootstrap-marketing-js.js",
    "build:critical-shell": "node scripts/build-critical-shell.js",
    "build:critical-css": "node scripts/build-critical-css.js",
    "build:ymo-css": "node scripts/build-ymo-css.js",
    "build": "npm run build:bootstrap && npm run build:bootstrap-js && npm run build:critical-css && npm run build:critical-shell && npm run build:ymo-css"
  },
  "devDependencies": {
    "bootstrap": "5.3.3",
    "esbuild": "^0.25.0",
    "fast-glob": "^3.3.2",
    "purgecss": "^6.0.0"
  }
}
